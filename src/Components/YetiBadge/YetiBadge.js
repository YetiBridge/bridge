import React from "react";
// import "./redAvaxLogo.svg";

const YetiBadge = ({ source, altText }) => {
  return <img src={source} alt={altText} />;
  // return (<a target="_blank" rel="noopener noreferrer" class="jss12 credit" href="https://www.avalabs.org/"><span class="title"></span><span class="name">Avalanche</span><img src="./avax_logo.svg" alt="Avalanche"></a>);
  {
    /* return <a onClick={this.handleClick} style={{cursor: 'pointer'}}>click me!</a>) */
  }
};

export default YetiBadge;
